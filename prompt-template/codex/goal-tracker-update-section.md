## Goal Tracker Update

If the round summary includes a **Goal Tracker Update Request** section, apply the requested changes to `{{GOAL_TRACKER_FILE}}`.

Review the requested changes carefully:
- Mark items as complete only if the work is genuinely done
- Add new sub-goals if the implementation revealed additional required tasks
- Update progress notes to reflect the current state

Apply all reasonable requests from the summary's Goal Tracker Update Request section.
