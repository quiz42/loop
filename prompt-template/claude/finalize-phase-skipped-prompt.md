# Finalize Phase (Review Skipped)

You have reached the Finalize Phase. The implementation review was skipped.

**Reason:** {{REVIEW_SKIP_REASON}}

Your task is to:

1. Review the entire implementation from start (`{{START_BRANCH}}`) to current state (`{{BASE_BRANCH}}`)
2. Verify that the project meets all goals in `{{PLAN_FILE}}`
3. Check the goal tracker at `{{GOAL_TRACKER_FILE}}` to confirm all objectives are satisfied
4. Write a finalize summary to `{{FINALIZE_SUMMARY_FILE}}` that describes:
   - What was accomplished
   - Final state of the implementation
   - Any remaining known issues or future work
   - Why the review was skipped

After writing the finalize summary, commit your changes. The loop will complete after finalization.
